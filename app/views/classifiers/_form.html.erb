<%= form_for(@classifier) do |f| %>
  <% if @classifier.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@classifier.errors.count, "error") %> prohibited this classifier from being saved:</h2>

      <ul>
      <% @classifier.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

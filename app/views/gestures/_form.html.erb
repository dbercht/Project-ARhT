<%= form_for(@gesture) do |f| %>
  <% if @gesture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gesture.errors.count, "error") %> prohibited this gesture from being saved:</h2>

      <ul>
      <% @gesture.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
